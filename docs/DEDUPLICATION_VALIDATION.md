# v0.2.2 验证记录

验证日期：2026-09-23。

## 自动化验证

- 27 个测试文件、258 项用例已验证。执行全量回归，并对迁移版本断言、离线评测结构适配及界面变更完成针对性复验。
- `npm run build`：TypeScript 与 Vite 生产构建通过。
- `git diff --check`、提交内容密钥检查通过。
- 新增测试覆盖精确跨来源复用、并发上传、来源恢复、共享来源变化拆分、最后来源移除、处理中旧任务保护、归一化合并后的再次上传、多成员合并、版本确认、撤销及过期冲突。
- 检索验证覆盖确认前去重、保持独立、分页总数、来源筛选、上下文与历史版本隔离。算法覆盖空正文、单元格边界、数值/单位/型号变化、不同客户项目保护，以及一万份资料的候选限量。
- 7 项界面渲染与契约测试覆盖主资料选择、差异标签、失效按钮、中文依据、来源下载和不安全链接过滤。本轮浏览器控制不可用，未宣称完成实际点击或像素布局验收。

主要命令：

```powershell
npm test -- --maxWorkers=1
npx vitest run tests/canonical-metadata.test.ts tests/database-lock.test.ts tests/evaluation-runner.test.ts tests/evaluation-size.test.ts tests/duplicates-ui.test.tsx --maxWorkers=1
npm run build
```

## 本地真实资料验证

升级前正常关闭数据库并备份；启动迁移后通过 HTTP 验证，不同时打开第二个数据库进程。

- 原有 225 份资料的原字段逐项一致，225 个当前来源引用完成回填。
- 107 条人工确认样例、11,738 个知识片段及结构化产品参数保持不变。
- 首轮扫描 196 份当前可用资料、2,833 个候选关系，得到 74 组建议：11 组完全重复、3 组内容重复、10 组疑似版本、50 组相似资料。
- 本轮触发单文档候选数保护上限，明确返回 `truncated=true`；这是限量候选扫描，不声称已穷举所有文档对。可以按来源、选中资料或筛选条件继续扫描。
- 所有建议保持待处理，没有自动合并、确认版本或删除原文件。扫描没有调用 Kimi。
- 抽查全文检索：默认只出现 active 主资料，单份资料最多 3 个片段；在同一查询下，去重与多样性处理后的结果总数为 433，显式查看全部片段为 7,314，分页计数与过滤一致。

公司文件、私有链接、密钥、密码、数据库备份和逐项本地验收数据均未提交仓库。
